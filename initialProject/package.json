{
  "name": "diploi-tiny-project",
  "version": "0.1.0",
  "scripts": {
    "start": "if [ ${STAGE} = \"production\" ]; then node server.mjs; else nodemon server.mjs; fi",
    "lint": "next lint"
  },
  "dependencies": {
    "nodemon": "2.0.20"
  }
}
